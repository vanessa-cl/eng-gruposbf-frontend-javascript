import styled from "styled-components";
import Link from "next/link";

const AccordionItem = styled(Link)`
  font-weight: normal;
  font-size: var(--nike-paragraph-medium: 16px);
  color: var(--nike-neutral-100);
`;

export default AccordionItem;