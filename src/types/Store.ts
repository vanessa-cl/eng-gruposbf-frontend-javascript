export type Store = {
  name: string;
  number: number;
  latitude: string;
  longitude: string;
  adress: string;
};

export type StoreItem = {
  name: string;
  number: number;
  adress: string;
  distance: number;
};
